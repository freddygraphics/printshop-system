Write-Host "=== 🚀 PrintShop Frontend Auto-Repair Script ===" -ForegroundColor Cyan

# Ir a la carpeta frontend
Set-Location "C:\Users\fredd\Downloads\printshop_full_starter\frontend"

Write-Host "🧹 Cleaning cache and dependencies..." -ForegroundColor Yellow
Remove-Item -Recurse -Force ".next" -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force "node_modules" -ErrorAction SilentlyContinue
Remove-Item "package-lock.json" -ErrorAction SilentlyContinue

Write-Host "📦 Reinstalling dependencies..." -ForegroundColor Green
npm install

Write-Host "🔍 Checking Next.js version..." -ForegroundColor Yellow
npm list next

Write-Host "✅ Rebuilding project..." -ForegroundColor Green
npm run build

Write-Host "🚀 Starting development server..." -ForegroundColor Cyan
npm run dev
